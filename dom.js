const $ = id => document.getElementById(id);

const bar = $("bar");
const wrapper = $("wrapper");
const closeOptions = $("closeOptions");
const toDoCardSection = $("toDoCardsSection");
const toDoCardForm = $("toDoCardForm")